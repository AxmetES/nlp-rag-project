### 1.0.0
### description
NLP RAG project based on PDF files table game tutorial.
game "Нордгард: Новые земли"

### prerequisites
```bash
git clone https://github.com/AxmetES/nlp-rag-project.git
```
```bash
cd nlp-rag-project
```

### Jupyter / EDA (build index locally)

The repository includes notebooks in `app/notebooks/`.

### 1) Create virtualenv & install dependencies
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

```bash
docker-compose up
```
